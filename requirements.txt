# Dependencias diretas do backend. As transitivas ficam por conta do pip.
# Versoes fixadas para que o ambiente seja reproduzivel.

# --- Banco de dados -------------------------------------------------------
SQLAlchemy==2.0.52
psycopg2-binary==2.9.12
# O checkpointer do LangGraph usa psycopg3, nao o psycopg2 do SQLAlchemy.
psycopg[binary,pool]==3.3.5

# --- Indexacao e busca (hrag.py, precompute_pipeline.py) ------------------
sentence-transformers==6.0.1
transformers==5.16.1
semchunk==4.1.1
PyPDF2==3.0.1

# --- Servidor MCP ---------------------------------------------------------
fastmcp==3.4.7

# --- Agente (juri.py) -----------------------------------------------------
langgraph==1.2.11
langgraph-checkpoint-postgres==3.1.2
langchain-core==1.6.1
langchain-ollama==1.1.0
langchain-mcp-adapters==0.3.2

# --- API HTTP (api.py) ----------------------------------------------------
fastapi==0.141.1
uvicorn==0.52.4
sse-starlette==3.4.10

# --- Utilidades -----------------------------------------------------------
pydantic==2.13.5
python-dotenv==1.2.3
